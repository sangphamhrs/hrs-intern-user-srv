Entity Framework Core migrations will be generated here. Use the following commands to create and apply migrations:

1. Add migration:
   dotnet ef migrations add InitialCreate --project UserService.csproj

2. Update database:
   dotnet ef database update --project UserService.csproj

You can run these commands in your terminal or use Visual Studio's Package Manager Console.
